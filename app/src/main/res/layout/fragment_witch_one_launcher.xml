<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    android:clickable="true">

    <Button
        android:id="@+id/start_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/start"
        android:textSize="@dimen/medium_size"
        android:textColor="@android:color/white"
        android:padding="@dimen/large_space"
        android:background="@drawable/button_draw"
        android:layout_margin="@dimen/large_space"
        android:visibility="invisible"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="@id/best_scores_btn"
        app:layout_constraintRight_toRightOf="@id/best_scores_btn"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/best_scores_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/best_score"
        android:textSize="@dimen/medium_size"
        android:textColor="@android:color/white"
        android:visibility="invisible"
        android:background="@drawable/button_draw"
        android:padding="@dimen/large_space"
        android:layout_margin="@dimen/large_space"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/start_btn" />

    <FrameLayout
        android:id="@+id/fragment_witch_one"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:elevation="@dimen/medium_space"></FrameLayout>

    <FrameLayout
        android:id="@+id/fragment_best_score"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:elevation="@dimen/small_space"></FrameLayout>


</android.support.constraint.ConstraintLayout>